import SwiftUI

struct TracksView: View {
    var body: some View {
        VStack {
            Text("Tracks")
                .font(.largeTitle)
                .padding()

            Spacer()
        }
        .navigationTitle("TracksView")
    }
}
